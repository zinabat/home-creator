/* eslint-disable no-unused-vars */
const aws = require('../aws.js');

module.exports.handler = function (req, res) {
	// todo: Validate that the file is a CSV file and is not corrupted
	// todo: upload to s3
};
